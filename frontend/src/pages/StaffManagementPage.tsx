import { FormEvent, useState } from 'react'
import { StaffShell } from '../components/StaffShell'

export function StaffManagementPage(){const [form,setForm]=useState({username:'',email:'',password:'',firstName:'',lastName:'',phoneNumber:'',role:'AGENT'});const [message,setMessage]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage('');setError('');try{const {apiRequest}=await import('../lib/api');const r=await apiRequest<{username:string}>('/api/staff/create',{method:'POST',body:JSON.stringify(form)});setMessage(`Staff account ${r.username} created successfully.`);setForm({username:'',email:'',password:'',firstName:'',lastName:'',phoneNumber:'',role:'AGENT'})}catch(e){setError(e instanceof Error?e.message:'Unable to create staff account')}finally{setBusy(false)}}return <StaffShell><div className="page-heading"><div><span className="eyebrow eyebrow--red">Administration</span><h1>Staff accounts</h1><p>Create operational accounts for agents or additional administrators.</p></div></div>{message&&<div className="success-banner">{message}</div>}{error&&<div className="form-error">{error}</div>}<div className="panel form-card"><form className="form-stack" onSubmit={submit}><div className="form-grid"><label>First name<input value={form.firstName} onChange={e=>setForm({...form,firstName:e.target.value})} required/></label><label>Last name<input value={form.lastName} onChange={e=>setForm({...form,lastName:e.target.value})} required/></label><label>Username<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/></label><label>Phone number<input value={form.phoneNumber} onChange={e=>setForm({...form,phoneNumber:e.target.value})}/></label><label>Role<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option>AGENT</option><option>ADMIN</option></select></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} minLength={8} required/></label></div><button className="button button--primary button--large" disabled={busy}>{busy?'Creating…':'Create staff account'}</button></form></div></StaffShell>}
