import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { AppShell } from '../components/AppShell'
import { StatusPill } from '../components/StatusPill'
import { getMyInquiries, getMyPolicies, getMyQuotations, type Inquiry, type Policy, type Quotation } from '../lib/api'
import { inquiryById, vehicleTitle } from '../lib/vehicle'
export function QuotationsPage(){const [items,setItems]=useState<Quotation[]>([]);const [inquiries,setInquiries]=useState<Inquiry[]>([]);const [policies,setPolicies]=useState<Policy[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{Promise.all([getMyQuotations(),getMyInquiries(),getMyPolicies()]).then(([quotes,records,issued])=>{setItems(quotes);setInquiries(records);setPolicies(issued)}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);const active=items.filter(q=>!policies.some(p=>p.quotationId===q.id));return <AppShell><div className="page-heading"><div><span className="eyebrow eyebrow--red">Motor insurance</span><h1>Quotations</h1><p>Review offers that still need your decision. Issued quotations move to your policies.</p></div></div>{error&&<div className="form-error">{error}</div>}{loading?<div className="loading-state">Loading quotations…</div>:<div className="stack-list">{active.map(q=>{const vehicle=vehicleTitle(inquiryById(inquiries,q.inquiryId));return <Link className="list-card" key={q.id} to={`/app/quotations/${q.id}`}><div><span className="eyebrow">{vehicle.cover} · {q.quoteReference||`Quotation #${q.id}`}</span><h2>{vehicle.registration} · {vehicle.makeModel}</h2><p>KES {Number(q.totalPayable??q.premiumAmount).toLocaleString()} total payable</p><small>Valid until {q.validUntil}</small></div><StatusPill value={q.status}/></Link>})}{!active.length&&<div className="panel empty-state--action"><p>No quotations need your decision right now.</p><Link className="button button--primary" to="/app/policies">View my policies</Link></div>}</div>}</AppShell>}
