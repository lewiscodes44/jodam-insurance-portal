import { useEffect, useState } from 'react'
import { AppShell } from '../components/AppShell'
import { StatusPill } from '../components/StatusPill'
import { getMyNotifications, type Notification } from '../lib/api'
export function NotificationsPage(){const [items,setItems]=useState<Notification[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{getMyNotifications().then(setItems).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <AppShell><div className="page-heading"><div><span className="eyebrow eyebrow--red">Updates</span><h1>Notifications</h1><p>Payment confirmations, policy updates and important messages from Jodam.</p></div></div>{error&&<div className="form-error">{error}</div>}{loading?<div className="loading-state">Loading notifications…</div>:<div className="stack-list">{items.map(n=><article className="list-card list-card--static" key={n.id}><div><span className="eyebrow">{n.channel}</span><h2>{n.subject||'Jodam Insurance update'}</h2><p>{n.message}</p><small>{new Date(n.createdAt).toLocaleString()} · {n.recipient}</small></div><StatusPill value={n.status}/></article>)}{!items.length&&<div className="panel empty-state">You're all caught up.</div>}</div>}</AppShell>}
