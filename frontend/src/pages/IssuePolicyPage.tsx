import { Link, useParams } from 'react-router-dom'
import { AppShell } from '../components/AppShell'
export function IssuePolicyPage(){const {id}=useParams();return <AppShell><div className="page-heading"><div><span className="eyebrow eyebrow--red">Quotation accepted</span><h1>Your policy is being prepared.</h1><p>Jodam staff will issue the policy from your approved quotation. Once it is ready, you will receive it here and can complete payment through M-Pesa.</p></div></div><div className="panel"><span className="eyebrow">What happens next</span><h2>Policy issuance and payment</h2><p className="detail-copy">We are preparing the policy terms from quotation #{id}. You do not need to submit anything else right now.</p><Link className="button button--primary" to="/app/notifications">View updates</Link></div></AppShell>}